
Clean ActionTypes later
Clean Attachments later


Fazer revisão:
  - ordem: hierarquia ficheiros

   : rectificar validation
   : connectar (e update ao ids.txt)
   : reformatar datas

         aapen_sluttdato
         format DD.MM.YYYY -> YYYY-MM-DD



fazer a equivalências de arbeidsgiver => perioder


. input
    onChange:



 grep -rho -e "'label:[^']*'" * | sort -u
