Todo:

1. na familierelasjon, pode ser que envie borSammen, etc com REPA/EKTE/etc. Confirmar que backend limpa

I have to decide once and for all what is the id (orgnr + stardato) and check validation when I duplicate them
=>  Editing arbeidsgiver / PeriodeMedForsikring thta is already selected, does NOT update the replySed

//  verificar estado dos utkasts, pode ser que tenha de apagar unsbquando arranco com a página


-> tenho de validar, ao meter um motregning novo, at least one boarn

"motregninger": [{
        "startdato": "2045-01-01",
        "sluttdato": "2046-01-01",
        "beloep": "200",
        "valuta": "NOK",
        "utbetalingshyppighet": "F002 barn2 motregning utbetalingshyppighet",
        "ytelseNavn": "F002 barn2 motregning ytelse",
        "mottakersNavn": "F002 barn2 motregning mottakersNavn",
        "vedtaksdato": "2047-01-01",
        "begrunnelse": "F002 barn2 motregning begrunnelse",
        "ytterligereInfo": "F002 barn2 motregning ytterligereInfo",
        "svarType": "anmodning_om_motregning_per_barn"
      }]

--> 400 login for Z992366

 grep -rho -e "'app:[^']*'" * | sort -u
 grep -rho -e "'label:[^']*'" * | sort -u
 grep -rho -e "'el:[^']*'" * | sort -u
 grep -rho -e "'message:[^']*'" * | sort -u

test:

25086820857 -> Q1
01026423488 m/inntekt i Q1
13077117754 m/inntekt og arbeidsperioder i Q1
04118107352 m/arbeidsperioder i Q1

ui improvs:
- useState lazy init
- rever slideAnimate 0.05
- meter keys para melhor ver o que muda

- accessibility
     - desligar css
     - div onclick /role=buttton
     - <div id="saveChanges" tabindex="0" role="button" aria-pressed="false">Save</div>
     -  label for?
     - usar listas
     - role=alert
       aria-checked="false"
        aria-label/ aria-labelledby
       aria-describedby attr
       required / aria-required.
       aria-invalid
       react-axe

- unit tests

Opprettsak:

--> key in select
--> when arbeidsgivere list is []
